apiVersion: apps/v1

kind: Deployment

metadata:
  name: flask-deployment
  # namespace: blog-app
  labels:
    app: flask-deployment

spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-pod

  template:
      metadata:
        name: flask-pod
        labels:
          app: flask-pod

      spec:
        containers:
          - name: flask-container
            image: coder336/flask-blog-app:b65c474547a174acd033a27554b0ecd520892c92
            imagePullPolicy: IfNotPresent
            ports:
              - containerPort: 5000
                # protocol: TCP
